apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: airtrail-db-pvc
  namespace: default
spec:
  accessModes:
      - ReadWriteOnce
  storageClassName: standard-rwo
  resources:
    requests:
      storage: 8Gi